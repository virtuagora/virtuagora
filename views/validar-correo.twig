{% extends "master.twig" %}

{% block titulo %}
{% if usuarioValido == true %}
Virtuagora - Correo verificado
{% else %}
Virtuagora - Verificacion incorrecta
{% endif %}
{% endblock %}



{% block body %}
<section id="expositor">
	<div class="row">
		<div class="medium-8 medium-centered large-6 large-centered columns">
			<h1 class="text-center logo-fuente">Virtuagora</h1>
		</div>
	</div>
</section>

<section id="passport">
	<div class="row">
		<div class="medium-8 medium-centered large-6 large-centered columns">
			{% if usuarioValido == true %}
			<div class="registro-exito-panel text-center">
				<i class="fa fa-envelope-o fa-5x"></i>
				<p class="welcome">Tu correo electronico ha sido verificado<br />
				<p><b>{{ nombre }} {{ apellido }}</b>, gracias por haber verificado que tu cuenta de correo electronico <b>{{ email }}</b>.</p>
				{% else %}
				<div class="verificacion-fail-panel text-center">

					<i class="fa fa-question-circle fa-5x"></i>
					<p class="welcome">Link de validación incorrecto</p>
					<p>El link ingresado es incorrecto. Verifique que el link que hemos enviado a su direccion de correo electronico es el mismo que ingresó en la barra de direcciones.</p> <p>Si sigue viendo este mensaje, comuniquese con los enviando un correo electronico a la dirección <b>admin@virtuagora.com</b>.</p>
					{% endif %}

					<div class="row">
						<div class="small-10 small-centered columns text-center">
							<a href="#" class="button  round success small">Volver a Virtuagora &raquo</a>
						</div>
					</div>
				</div>
			</div>
		</div>
		</section>
	{% endblock %}

	{% block scripts %}
	{% endblock %}
