{# VARIABLES PARA FORMATO DE MASTER-BODY.TWIG #}
{% set url_atras = '#' %}
{% set i_class_titulo = 'fa fa-leaf fa-fw' %}
{% set titulo = 'Contenidos creados y demases' %}
{% set comments = false %}

{# VARIABLES DE LA VISTA #}

{# ---------------------- #}

{% extends "master-body.twig" %}

{% block titulo %}
Virtuagora - Cambiar clave
{% endblock %}

{% block contenido %}

<h1>Contenidos</h1>
<table>
    <thead>
        <tr>
            <th class="text-center">Título</th>
            <th class="text-center">Autor</th>
            <th class="text-center">Fecha</th>
        </tr>
    </thead>
    <tbody>
        {% for con in contenidos %}
            <tr>
                <td><a href='{{ baseUrl }}/{{ con.contenible_type|lower }}/{{ con.contenible_id }}'>{{ con.titulo }}</a></td>
                <td>{{ con.autor.nombre }} {{ con.autor.apellido }}</td>
                <td>{{ con.created_at|date("m/d/Y") }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
<h3>Otros</h3>
{% if esModerador %}
    <a href="{{ baseUrl }}/admin/organismos">Administrar organismos.</a><br>
{% endif %}
{% if esFuncionario %}
    <a href="{{ baseUrl }}/crear/propuesta">Crear propuesta.</a><br>
    <a href="{{ baseUrl }}/crear/partido">Crear partido.</a><br>
    <a href="{{ baseUrl }}/partido">Listar partidos.</a><br>
{% endif %}
    <a href="{{ baseUrl }}/crear/problematica">Crear problematica.</a><br>
{% endblock %}
